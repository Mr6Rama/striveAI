{
  "name": "striveai-refactor",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node backend/server.js",
    "dev": "node --watch backend/server.js",
    "smoke": "node scripts/smoke.js",
    "build": "esbuild frontend/app.js --bundle --format=iife --platform=browser --target=es2020 --outfile=frontend/bundle.js",
    "vercel-build": "npm run build"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "express-rate-limit": "^7.4.1",
    "firebase-admin": "^13.10.0"
  },
  "optionalDependencies": {
    "nodemailer": "^6.9.13"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
